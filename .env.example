# .env.example

# Example API keys (replace with actual values in the local .env file)
TINYFY_API_KEY="your_actual_tinyfy_api_key"
GITHUB_TOKEN="your_github_token"

# Add any other environment variables used in your project
# Example database credentials (remove if not relevant to your project)
DB_HOST="localhost"
DB_USER="your_db_user"
DB_PASSWORD="your_db_password"

